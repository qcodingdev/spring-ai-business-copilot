package dev.qcoding.businesscopilot.supportcopilot.draft;

/** Explicit reply draft lifecycle. */
public enum SupportDraftStatus {
    DRAFTED,
    NEEDS_REVIEW,
    CONFIRMED,
    CANCELED,
    EXPIRED
}
