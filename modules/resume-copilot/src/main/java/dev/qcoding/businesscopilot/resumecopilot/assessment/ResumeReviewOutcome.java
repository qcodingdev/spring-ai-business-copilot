package dev.qcoding.businesscopilot.resumecopilot.assessment;

/** Human decision about the usefulness of an assessment draft, never a hiring decision. */
public enum ResumeReviewOutcome {
    ACCEPTED,
    EDITED_ACCEPTED,
    REJECTED
}
