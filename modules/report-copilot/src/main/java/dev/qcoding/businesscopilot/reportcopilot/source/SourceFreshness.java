package dev.qcoding.businesscopilot.reportcopilot.source;

/** Freshness decision calculated from immutable source observation metadata. */
public enum SourceFreshness {
    FRESH,
    STALE,
    UNKNOWN
}
