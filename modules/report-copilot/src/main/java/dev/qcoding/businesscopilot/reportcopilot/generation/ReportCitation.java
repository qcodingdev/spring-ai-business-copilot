package dev.qcoding.businesscopilot.reportcopilot.generation;

/** Explains why one source supports the generated report candidate. */
public record ReportCitation(String sourceId, String reason) {
}
