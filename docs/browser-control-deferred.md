# 浏览器控制延期说明

2.3.0 不引入浏览器自动化 Agent、通用工具编排或用户浏览器控制。Playwright
只用于本仓库 UI 的确定性测试和虚构数据截图，不作为运行时依赖，也不会访问用户
登录态、Cookie 或第三方页面。

如果未来评估浏览器控制，必须另立版本范围，先完成域名/动作 allowlist、人工确认、
凭据隔离、下载与上传边界、审计、失败恢复和供应商条款评审；不能复用普通 REST
连接的授权假设。
